# Local development broker for the MicroHydros simulator/prototype.
# Anonymous + unencrypted, matching docs/technical-solutions.md's note
# that auth/TLS is a "if time permits" stretch goal, not the current plan.
# Do not reuse this config outside local development.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
